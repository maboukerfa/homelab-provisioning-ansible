# Copy to .env on the HOST at /opt/silverbullet/.env (root:root, 0640).
# Never commit the real file -- .gitignore excludes it.
#
# The only credential on the instance. The compose file fails to start rather
# than exposing an unauthenticated SilverBullet if this is unset.
SB_USER=silverbullet:changeme
