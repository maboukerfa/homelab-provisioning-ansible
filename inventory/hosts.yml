---
all:
  children:
    homelab:
      hosts:
        # Rename this to whatever the box is actually called and point
        # ansible_host at its LAN address or DNS name. Everything else in the
        # repo refers to the `homelab` group, not to this host name, so adding a
        # second machine later means adding a line here and nothing else.
        homelab01:
          ansible_host: 192.168.1.10
